# Auto detect text files and perform LF normalization
* text=auto eol=lf

# Force Unix line endings for shell scripts and Docker entrypoints
*.sh text eol=lf
*.conf text eol=lf
Dockerfile text eol=lf
